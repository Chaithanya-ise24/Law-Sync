info:
  name: Analyse document
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/upload
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
